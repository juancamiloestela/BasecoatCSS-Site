---
title: Typography
sections: 
    - name: Headings
    - name: Inline Elements
    - name: Lists
    - name: Blockquotes
    - name: Preformatted & Code
    - name: Links
    - name: Badges
    - name: Icons
    - name: Helpers
---

<div class="layout-primary">
    <div class="layout-content-fluid">
        <div class="seven columns">
            <div>&nbsp;</div>
            <div class="seven desktop-six wide">
                <h1 class="section-title">
                    Typography
                    <small>Great content deserves great typography</small>
                </h1>
            </div>
        </div>
        <div class="seven columns">
            <div>
                <ul class="unstyled right-aligned desktop-only">
                    {{#each sections}}
                    <li><a href="#{{chain 'lowercase' 'hyphenate' name}}">{{ name }}</a></li>
                    {{/each}}
                </ul>
                <a href="#" class="peek-toggle-left desktop-hide">Sections</a>
            </div>
            <div class="seven desktop-six wide">

                <p>Typography in Basecoat is king. All elements are carefully styled to match vertical rythms and ensure optimum readability. All dimensions are handled in <code>ems</code> so typography controls the dimensions of all other elements, even if you extend Basecoat to use other font families. All containers are targeted to have a <code>max-width</code> of <code>50em</code>, this way each line of text will be the ideal width so that users can easily find the beginning of the next.</p>

                <p>By default the font family used is <em>sans-serif</em> but you can easiy change the font in the <code>&lt;body&gt;</code> element and all elements will update accordingly.</p>

                {{#each sections}}
                    {{ partial name }}
                {{/each}}
            </div>
        </div>
    </div>
</div>
